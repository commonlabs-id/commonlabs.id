import styled from '@emotion/styled';

const HeroWrapper = styled('header')`
  display: flex;
  flex-direction: row;
  align-items: center;
  flex: 1;
  position: relative;
  padding: 1.5rem;
`;

export default HeroWrapper;
